apiVersion: v1
kind: Service
metadata:
  name: pump-coaching-service
spec:
  selector:
    app: pump-coaching-service
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP

# internal DNS name http://pump-coaching-service:8080
